APP_ENV=development
LOG_LEVEL=debug
DEV_UI_ENABLED=true

# This example does not use a database, but every application needs a private
# signing key when it adds authentication later.
JWT_SECRET=replace-with-a-random-secret-of-at-least-32-characters
